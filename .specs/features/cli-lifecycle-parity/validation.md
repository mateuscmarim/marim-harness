# Validation: CLI lifecycle parity — PASS

All 35 acceptance criteria have concrete outcome evidence after review fixes. Three isolated behavior mutations were killed. No implementation or test file was changed by this Verifier.

- Date: 2026-09-14.
- Source of truth: `.specs/features/cli-lifecycle-parity/spec.md`, LIFE-01 through LIFE-35.
- Verifier: independent Phase 3 sub-agent, author != verifier; actual implementation trees read-only.
- Harness diff: `ff9c58d7..9be384fd2192be38e46981eb8f17ac69ed6ed6b7` on `feat/cli-lifecycle-parity`, plus the reviewed uncommitted capability/API/roadmap documentation.
- Android diff: `a928c48..9154b24`. The `mobile/` evidence prefix below means `/home/mateuscmarim/Projects/marim.dev/marim-mobile/`.
- Sensor source revision: `173075e6543eea0edb028cc025bb58e840295e7a`. Later commit `9be384fd` only adds five lines of exact rendered-message assertions; production source is identical.

## Task completion

T1 through T7 are implemented and their relevant gates pass, including T6 at Android commit `9154b24`. T8's independent acceptance review and sensor are complete. The orchestrator owns the remaining task/spec status bookkeeping and acceptance-document commit; this report does not edit those artifacts or authorize publishing/merging/deployment.

## Spec-anchored acceptance criteria

Citations below name actual assertions rather than test names alone. Multi-clause outcomes use combined component and integration evidence. LIFE-35 is checked against its required documentary fields. No unsupported transport capability is used to waive another criterion.

| Requirement | Spec-defined outcome | Evidence and assertion | Verdict |
| --- | --- | --- | --- |
| LIFE-01 | Completed backend compaction is identified and published before the same turn completes. | `tests/test_claude_lifecycle.py:166` — `assert seen == [("Claude compacted its context", ContextReport(100, None))]`; `tests/test_codex_lifecycle.py:25` — `assert first[0].message == "Codex compacted its context"`; `tests/test_lifecycle_delivery.py:273` — `assert notice.seq < terminal.seq` | PASS |
| LIFE-02 | Fallback/reroute message names supplied old and new models. | `tests/test_claude_lifecycle.py:46` — `assert notice.message == "Claude model fallback: one → two"`; `tests/test_codex_lifecycle.py:54` — `assert note.message == "Codex model rerouted: a → b"` | PASS |
| LIFE-03 | Supported notices retain their kind and available message. | `tests/test_codex_lifecycle.py:143` — `assert payload == {message:"reason", backend:"codex-cli", kind:method, severity:"warning", id:notice.id, data:{}}`; `tests/test_claude_lifecycle.py:47` — `assert notice.kind == "model_refusal_fallback"`; `tests/test_lifecycle_delivery.py:520` — `assert notice.data["message"] == "valid notice"` | PASS |
| LIFE-04 | Retry notice does not finish the turn; its real terminal event still completes it. | `tests/test_codex_lifecycle.py:64` — `assert notes[0].message == "Codex is retrying: busy"`; `tests/test_codex_lifecycle.py:65` — `assert state.done is None`; `tests/test_codex_lifecycle.py:69` — `assert state.done.status == "completed"` | PASS |
| LIFE-05 | Both TUI paths render the supplied message as a separate notice widget. | `tests/test_lifecycle_delivery.py:91` — `assert widget types == [AssistantMessage, NoticeMessage, AssistantMessage]`; `tests/test_lifecycle_delivery.py:101` — `assert rendered == ["· same message", "· same message"]`; `tests/test_lifecycle_delivery.py:342` — `assert _texts(app, NoticeMessage) == ["· remote lifecycle", "· remote lifecycle"]` | PASS |
| LIFE-06 | Every headless output format puts notices on supplied stderr and excludes them from stdout. | `tests/test_lifecycle_delivery.py:73` — `assert err.getvalue() == "notice only\n"`; `tests/test_lifecycle_delivery.py:74` — `assert "notice only" not in out.getvalue()` | PASS |
| LIFE-07 | The sequenced additive event preserves the old message-only wire contract. | `tests/test_lifecycle_delivery.py:55` — `assert event.data == notice.to_payload()`; `tests/test_lifecycle_delivery.py:56` — `assert event.seq < terminal.seq`; `tests/test_lifecycle_delivery.py:33` — `assert wire.message == notice.message`; `tests/test_lifecycle_delivery.py:35` — `assert isinstance(old, SessionNotice)` | PASS |
| LIFE-08 | Mobile stores the event before delivery/rendering through the existing Room path. | `mobile/app/src/test/java/dev/marim/mobile/data/repo/TranscriptWsTest.kt:679` — `assertEquals(listOf(cached.seq, 7L), rows.map { it.seq })`; `:681` asserts `session.notice`; `:684` asserts the rendered message. Repository `mobile/app/src/main/java/dev/marim/mobile/data/repo/TranscriptRepository.kt:751` retains `dao.insertAndPrune(...)`. | PASS |
| LIFE-09 | A duplicate sequence produces one persisted and rendered notice. | `mobile/app/src/test/java/dev/marim/mobile/data/repo/TranscriptWsTest.kt:704` — `assertEquals(1, rows.count { it.type == "session.notice" })`; `:705` — `assertEquals(listOf("Retrying"), mappedNoticeTexts)`. | PASS |
| LIFE-10 | Backend compaction never clears the mirror/history cache. | `tests/test_lifecycle_delivery.py:57` — `assert not any(e.type == "compaction.finished" for e in events)`; `mobile/app/src/test/java/dev/marim/mobile/data/repo/TranscriptWsTest.kt:680` — `assertEquals(cached, rows.first())`; `:688` — `assertEquals(1, server.requestCount)`. | PASS |
| LIFE-11 | A valid post-compaction used count replaces the old count while preserving the last window. | `tests/test_claude_lifecycle.py:248` — `assert model.context_report == ContextReport(100, 1000)`; `tests/test_codex_lifecycle.py:131` — `assert state.context == ContextReport(100, 1000)` | PASS |
| LIFE-12 | Unknown used count suppresses persisted fallback until a fresh valid reading restores it. | `tests/test_claude_lifecycle.py:195` — `assert current_context_report(model, history) is None`; `tests/test_claude_lifecycle.py:255` — `assert current_context_report(model, []) == ContextReport(120, 1000)`; `tests/test_codex_lifecycle.py:67` — `assert seen == [None] and state.context is None and state.done is None` | PASS |
| LIFE-13 | Thinking estimate is separate, unbilled, current-turn telemetry and resets remotely on the next turn. | `tests/test_lifecycle_inventory.py:67` — `assert label == "thinking ~123 · backend requires action"`; `tests/test_lifecycle_inventory.py:59` — `assert info.usage.input_tokens == 17`; `tests/test_lifecycle_delivery.py:566` — `assert info.backend_telemetry["thinking_tokens"] is None` | PASS |
| LIFE-14 | Backend state is an observation, not a terminal result or mode switch. | `tests/test_claude_lifecycle.py:57` — `assert state.backend_state == "requires_action"`; `tests/test_lifecycle_inventory.py:57` — `assert info.backend_telemetry == {"state": "idle", "thinking_tokens": 900}`; `tests/test_lifecycle_inventory.py:58` — `assert info.mode == "ask"`; `tests/test_lifecycle_delivery.py:210` — `assert not any(e.type in ("ask.resolved", "turn.finished") for e in events)` | PASS |
| LIFE-15 | A pending brokered ask keeps its identifier and unresolved future. | `tests/test_lifecycle_delivery.py:207` — `assert host.pending_asks() == before`; `tests/test_lifecycle_delivery.py:208` — `assert host.pending_asks()[0]["id"] == ask.id`; `tests/test_lifecycle_delivery.py:209` — `assert not ask.future.done()` | PASS |
| LIFE-16 | Stale events preserve current-turn completion/approval ownership. | `tests/test_codex_lifecycle.py:61` — `assert _outputs("thread/compacted", {"turnId": "old"}, state, "current") == []`; `tests/test_codex_server.py:88` — `assert handle.current_turn_id == "turn-2"`; `tests/test_lifecycle_delivery.py:209` — `assert not ask.future.done()`; `tests/test_claude_process.py:473` — `assert second[0]["type"] == "user" and second[-1]["result"] == "b"`. | PASS |
| LIFE-17 | A backend VCS event refreshes the existing local git view for its own workspace without checkpoint writes. | `tests/test_lifecycle_delivery.py:284` — `assert any(snapshot.vcs_revision == 1 and snapshot.seq < terminal.seq)`; `tests/test_lifecycle_inventory.py:234` — ``assert "`after`" in widget.text and "`before`" not in widget.text``; `tests/test_lifecycle_inventory.py:239` — `assert app.harness.checkpoints.list() == before_checkpoints`; `tests/test_lifecycle_inventory.py:240` — `assert git("for-each-ref", "refs/marim").stdout == ""` | PASS |
| LIFE-18 | A stable session/task identifier opens one shell activity. | `tests/test_claude_lifecycle.py:88` — `assert started.task_id == "claude:s:shell" and started.status == "running"`; `tests/test_lifecycle_delivery.py:144` — `assert len(app.stream.backend_tasks) == 1` | PASS |
| LIFE-19 | Progress/completion update the same task/card instead of appending cards. | `tests/test_claude_lifecycle.py:92` — `assert progress.task_id == started.task_id and progress.status == "running"`; `tests/test_claude_lifecycle.py:101` — `assert finished.task_id == started.task_id and finished.status == "completed"`; `tests/test_lifecycle_delivery.py:144` — `assert len(app.stream.backend_tasks) == 1`; `tests/test_lifecycle_delivery.py:147` — `assert widget.result_text == "testing (interrupted)"` | PASS |
| LIFE-20 | Agent task traffic remains separate from background shells. | `tests/test_claude_lifecycle.py:76` — `assert consume(local_agent_task) == []`; `tests/test_lifecycle_delivery.py:149` — `assert app.stream.subagents == []`; `tests/test_lifecycle_delivery.py:167` — `assert routed.stream_id == "child"` | PASS |
| LIFE-21 | Requested closure interrupts live shells; unexpected nonzero exit fails them. | `tests/test_claude_lifecycle.py:233` — `assert states == [("claude:s:shell", "running"), ("claude:s:shell", "interrupted")]`; `tests/test_claude_lifecycle.py:296` — `assert states == [("claude:s:job", "running"), ("claude:s:job", "failed")]` | PASS |
| LIFE-22 | Backend-labelled inventory reaches session detail and existing TUI inventory views. | `tests/test_lifecycle_inventory.py:30` — `assert fields["backend_inventory"] == info.backend_inventory`; `tests/test_claude_lifecycle.py:67` — `assert state.inventory["mcp_servers"] == [{"name": "docs", "status": "connected"}]`; `tests/test_lifecycle_inventory.py:93` — ``assert "Declared tools: `mcp__search`" in help_text``; `tests/test_lifecycle_inventory.py:96` — ``assert "Agents: `explore`" in help_text``; `tests/test_lifecycle_inventory.py:97` — ``assert "Backend permission mode: `default`" in help_text``; `tests/test_lifecycle_inventory.py:99` — ``assert "`search` — failed" in app.post_system.call_args.args[0]`` | PASS |
| LIFE-23 | New inventory replaces old process state and does not merge stale entries. | `tests/test_claude_lifecycle.py:69` — `assert state.inventory["tools"] == ["Bash"]`; `tests/test_claude_lifecycle.py:70` — `assert state.inventory["slash_commands"] == []`; `tests/test_lifecycle_inventory.py:56` — `assert info.backend_inventory == {"tools": ["new"]}`; `tests/test_lifecycle_inventory.py:36` — `assert info.backend_inventory == {}` | PASS |
| LIFE-24 | Marim built-ins retain precedence over backend names. | `tests/test_lifecycle_inventory.py:90` — `app.reset_conversation.assert_awaited_once()`; `tests/test_lifecycle_inventory.py:94` — ``assert "`/clear` — marim command takes precedence" in help_text`` | PASS |
| LIFE-25 | Only supported nonterminal backend commands forward exact prompt text; queue/compaction rules remain. | `tests/test_lifecycle_inventory.py:171` — `app.start_turn.assert_awaited_once_with("/review  preserve  spaces")`; `tests/test_lifecycle_inventory.py:175` — `app.queue.enqueue.assert_called_once_with("/review queued", None)`; `tests/test_lifecycle_inventory.py:184` — `assert app.start_turn.await_count == app.queue.enqueue.call_count == 1` | PASS |
| LIFE-26 | Normal persistence stores ordered normalized notice records outside model-facing prose. | `tests/test_cli_lifecycle.py:25` — `assert [p.content for p in parts] == ["before", "", "", "after"]`; `tests/test_cli_lifecycle.py:29` — `assert "".join(p.content for p in parts) == "beforeafter"`; `tests/test_lifecycle_delivery.py:280` — `assert persisted == [notice.data]` | PASS |
| LIFE-27 | Local/attached TUI and mobile restore notices once in activity order. | `tests/test_lifecycle_delivery.py:117` — `assert order_response_parts(parts) == parts`; `tests/test_lifecycle_delivery.py:122` — `assert isinstance(mounted[1], NoticeMessage)`; `tests/test_lifecycle_delivery.py:341` — `assert app.history_messages == link.messages`; `mobile/app/src/test/java/dev/marim/mobile/domain/TranscriptMapperNoticeTest.kt:44` — `assertEquals([before,Compacted,Compacted,after,Compacted], mappedTexts)`; `:73` asserts both real tool returns and the intervening note. | PASS |
| LIFE-28 | Stable identity deduplicates history/live overlap; identical text with distinct IDs survives. | `tests/test_lifecycle_delivery.py:100` — `assert len(log.query(NoticeMessage)) == 2`; `tests/test_lifecycle_delivery.py:342` — `assert _texts(app, NoticeMessage) == ["· remote lifecycle", "· remote lifecycle"]`; `mobile/app/src/test/java/dev/marim/mobile/domain/TranscriptMapperNoticeTest.kt:44` — exact five-item order; `:54` — five distinct item keys. | PASS |
| LIFE-29 | Normalized result details survive stats persistence without changing exact cost/token accounting. | `tests/test_lifecycle_stats.py:34` — `assert backend_result == {num_turns:2,duration_api_ms:350,stop_reason:"end_turn",permission_denials:[{tool_name:"Bash",tool_use_id:"t"}]}`; `tests/test_lifecycle_stats.py:40` — `assert (event.input_tokens, event.output_tokens, event.cost_usd) == (100, 20, 0.25)`; `tests/test_lifecycle_stats.py:53` — `assert "PRIVATE" not in ledger.workspace_path.read_text()` | PASS |
| LIFE-30 | An older ledger without lifecycle metadata retains identical totals. | `tests/test_lifecycle_stats.py:45` — `assert restored is not None and restored.backend_result is None`; `tests/test_lifecycle_stats.py:46` — `assert (restored.input_tokens, restored.output_tokens, restored.cost_usd) == (100, 20, 0.25)` | PASS |
| LIFE-31 | Malformed/unknown optional payloads are ignored and subsequent valid turns complete. | `tests/test_claude_lifecycle.py:33` — `assert state.consume({"subtype": "unknown"}) == []`; `tests/test_lifecycle_delivery.py:518` — `assert terminal.data["output"] == "completed despite malformed notice"`; `tests/test_lifecycle_delivery.py:520` — `assert notice.data["message"] == "valid notice"`; `tests/test_lifecycle_delivery.py:522` — `assert not any(e.type == "turn.error" for e in events)` | PASS |
| LIFE-32 | Optional callback/refresh failures retain execution and log operation/backend without payload. | `tests/test_cli_lifecycle.py:38` — `assert "codex-cli" in caplog.text`; `tests/test_cli_lifecycle.py:41` — `assert "private notice" not in caplog.text`; `tests/test_lifecycle_inventory.py:113` — `assert "ClaudeAdapter backend_inventory refresh failed" in caplog.text`; `tests/test_lifecycle_inventory.py:114` — `assert "PRIVATE" not in caplog.text`; `tests/test_lifecycle_delivery.py:229` — `assert message.parts[1].content == "still working"`; `tests/test_lifecycle_stats.py:71` — `assert event.input_tokens == 20 and event.backend_result is None` | PASS |
| LIFE-33 | Child lifecycle notices use child streams and shared live/history identity. | `tests/test_lifecycle_delivery.py:165` — `assert remainder is None`; `tests/test_lifecycle_delivery.py:167` — `assert routed.stream_id == "child"`; `tests/test_lifecycle_delivery.py:172` — `assert notice_from_part(message.parts[0])["id"] == routed.event.id`; `tests/test_lifecycle_delivery.py:192` — `assert notice_from_part(message.parts[0])["id"] == notice.id == "same-id"` | PASS |
| LIFE-34 | Global warnings reach matching backend connection sessions once, not unrelated connections. | `tests/test_codex_lifecycle.py:84` — `assert handle.events.get_nowait() == ("configWarning", {"summary": "bad config"})`; `tests/test_codex_lifecycle.py:85` — `assert handle.events.empty()`; `tests/test_codex_lifecycle.py:86` — `assert remote.events.empty()` | PASS |
| LIFE-35 | Unavailable/conditional capabilities explicitly record event, evidence, version and fallback. | `docs/reference/cli-lifecycle-capabilities.md:3` pins Claude 2.1.270 / Codex 0.154.0; `:10` names installed-schema/emitter evidence; `:30` excludes internal model_fallback; `:31` records absent status/compact-result delivery and fallback; `:33` records conditional session_state_changed. Generated public Codex schemas were independently inspected for all seven lifecycle notification families. This is a documentation obligation; these concrete fields are the checked outcome, not a paid/live-event claim. | PASS |

**Coverage:** 35/35 acceptance criteria have outcome evidence. No unresolved spec-precision gap.

## Edge cases

- Compaction start is not success: `tests/test_codex_lifecycle.py:23` asserts the started event yields `[]`; completion is asserted separately at line 25. Retry and terminal state remain separate at lines 64–69.
- Repeated compactions preserve occurrences: `tests/test_codex_lifecycle.py:30` asserts distinct notice IDs while duplicate modern/legacy representations yield no extra notices. `tests/test_cli_lifecycle.py:27` asserts identical-message occurrences have different IDs.
- Adapter adoption keeps callback/process ownership: `tests/test_lifecycle_delivery.py:439` — `assert new._process is process and process.alive`; `tests/test_lifecycle_delivery.py:440` — `assert not any(isinstance(e, BackendNotice) for e in old_events)`; `tests/test_lifecycle_delivery.py:443` — `assert notice.id == "claude:adopted-session:adopted-notice"`.
- Cancellation preserves observed notices in resumable history: `tests/test_lifecycle_delivery.py:378` — `assert finished.data["interrupted"] is True`; `tests/test_lifecycle_delivery.py:382` — `assert markers == [delivered.data]`.
- Message-only older events still render and malformed metadata does not prevent following assistant output: `mobile/app/src/test/java/dev/marim/mobile/domain/TranscriptMapperNoticeTest.kt:98` asserts two older-event notes and line 99 asserts `still works`.
- Invalid numeric telemetry is rejected by `src/marim_harness/config/lifecycle.py:107` (`nonnegative_int` accepts only nonnegative integers excluding bool); Boolean rejection is asserted at `tests/test_claude_lifecycle.py:54`, negative display rejection at `tests/test_lifecycle_inventory.py:71`, and invalid duration/count metadata at `tests/test_lifecycle_stats.py:47`. Strings and non-finite floats cannot pass the integer type gate.
- Denied work remains separate from shell progress: pending ask/future assertions in LIFE-15, unchanged checkpoint/ask assertions in LIFE-17, and the shell renderer's independent card dictionary in LIFE-19 verify the separate ownership paths. A zero-token permission-denied result is retained at `tests/test_lifecycle_stats.py:100` without adding tokens at line 101.
- Reasoning before/after a notice keeps its order even when Codex reuses an item ID: `tests/test_codex_lifecycle.py:187` asserts `["before", "", "after", "done"]` and line 193 verifies the intervening notice.

## Gate evidence

The orchestrator executed the repository Build gate in CI order using the prepared uv environment: `uv run ruff check src tests`, `uv run ruff format --check src tests`, `uv run pyright`, `uv run pytest`, then `uv build`. The Verifier independently inspected the final test/build logs.

- Ruff check passed; formatter checked 578 files; Pyright reported 0 errors.
- Harness: 4899 passed, 0 failed, 9 skipped, 18 warnings; 95.30% coverage, above the 90% gate. Final log: `scratchpad/final-tests.log` outside the repository.
- Wheel and sdist build succeeded: `scratchpad/final-build.log` records both artifacts.
- The final raw VCS fixture and its exact pre-terminal revision assertion were added to an existing test after the full suite. Both parametrized backend cases passed; Ruff/format passed again. No production source changed after the full suite.
- Test-only commit `9be384fd` strengthened local/attached message-value assertions; both affected tests passed. No test was deleted or disabled.
- Android: `./gradlew ktlintCheck detekt`, `./gradlew testDebugUnitTest`, and `./gradlew :app:assembleDebug` passed. Independent XML inspection found 84 suites, 624 tests, 0 failures/errors/skips; debug APK exists.
- Independent `git diff --check` passed.

The nine skips are pre-existing opt-in live tests, consistent with this task's prohibition on paid/live CLI calls: four Codex tests at `tests/test_codex_live.py:67`, `:84`, `:107`, `:125` require `MARIM_LIVE_CODEX=1`; three Claude tests at `tests/test_claude_cli_live.py:46`, `:73`, `:91` require `MARIM_LIVE_CLAUDE=1`; `tests/test_openrouter_cost_live.py:23` and `tests/test_live_smoke.py:32` require an exported OpenRouter key. No feature test was skipped.

Test integrity was independently compared by AST over baseline and final tests: 4592→4645 named test definitions, 53 added, 0 removed. Collected parametrized-case counts are distinct from definition counts; the baseline collected count was not captured. Diff inspection found only additive contract expectation changes to existing tests, including the three new stream-event names, plus the intentional change from a compaction-start notice to a completed-compaction notice. Assertions were not weakened to hide a failure.

## Discrimination sensor

An isolated `scratchpad/verifier-mutations` copy of `src`, `tests` and the test configuration was created from the actual reviewed revision. `marim_harness.__file__` was checked to resolve inside that copy. The three selected unmodified controls passed before mutations.

Each test ran with `PYTHONPATH=<isolated>/src UV_CACHE_DIR=<scratchpad>/uv-cache uv run --no-project --python /home/mateuscmarim/Projects/marim.dev/marim-harness/.venv/bin/python python -m pytest --no-cov -n 0 -q <test-id>`. Each source copy was read, mutated, tested and restored before the next fault. No stash was used.

| Fault | Mutated behavior | Detecting assertion | Outcome |
| --- | --- | --- | --- |
| Retry terminal inversion | `src/marim_harness/codex/turn.py:107`: `if not item.will_retry` → `if item.will_retry` | `tests/test_codex_lifecycle.py:65`: `assert state.done is None` fails because retry now terminates the turn. | Killed, pytest exit 1 |
| Remove occurrence claim | `src/marim_harness/interfaces/tui/session_view.py:111`: remove `self._notice_ids.add(identity)` | `tests/test_lifecycle_delivery.py:100`: `assert len(log.query(NoticeMessage)) == 2` fails with 3. | Killed, pytest exit 1 |
| Lose context window | `src/marim_harness/codex/turn.py:114`: retain `None` instead of `state.context.window` | `tests/test_codex_lifecycle.py:131`: `assert state.context == ContextReport(100, 1000)` fails with window None. | Killed, pytest exit 1 |

**Sensor outcome:** 3/3 killed, 0 survived; lightweight feature-level depth. The first runner attempt saw the same correct assertion failures but misread the quiet pytest footer; its scratch-only result matcher was corrected to recognize the exact failed test ID, and the complete sensor was repeated successfully. This was a verifier-tool bookkeeping fault, not a surviving implementation mutant.

The scratch copy was deleted after each complete run. Both actual repositories' `git status --porcelain` and SHA-256 hashes of every tracked file were identical before/after each sensor run. Evidence is in `scratchpad/sensor-baseline.json`, `scratchpad/sensor-results.json` and `scratchpad/sensor-*.log`. The later explicitly identified test-only author commit happened after isolation verification. This report is the Verifier's only repository write.

## Ranked findings resolved before this verdict

1. LIFE-11/12: Codex invalidation discarded a valid context window. Independent probe reproduced `ContextReport(100,None)` after compaction plus fresh usage. The corrected implementation keeps the window separately and has an exact regression assertion; the sensor kills its removal.
2. LIFE-13: resetting thinking telemetry only inside the adapter left attached clients showing the previous turn's estimate. The adapter now publishes the reset; the two-turn host/RemoteInfo test asserts `None` on turn 2.
3. LIFE-17: incrementing a VCS revision and manually reopening `/worktree` did not prove event-triggered refresh. The existing displayed local widget now refreshes from the owning workspace; raw event→host revision and host→same-widget tests cover the complete path and no checkpoint mutation.
4. LIFE-21: process closure always marked tasks interrupted. Closure now distinguishes requested stop from unexpected nonzero exit; both statuses are tested.
5. LIFE-03: Codex guardian/config/deprecation notices lost their distinct kinds. Exact normalized payload assertions now retain the source kind.
6. LIFE-05/27/28/31 and edge coverage: added exact local/attached message rendering, attached replay overlap, malformed-event continuation, process adoption and cancellation persistence proofs. The author also detected and fixed reused reasoning-item ordering around a notice.

No ranked gap remains open. These corrected findings are grounded lesson candidates for the orchestrator; the Verifier does not edit lesson/state/spec files under its read-only assignment.

## Code quality and limits

The diff remains within the approved lifecycle, inventory, transcript and stats scope. It reuses activity callbacks, ordered history metadata, session wire events, existing TUI widgets and Room persistence. No new Android screen, offline mutation queue, private backend command dependency, tool execution path or checkpoint write was introduced. Domain tests cover normalized outcomes; integration tests cover host sequencing, remote rendering, persistence, callback errors and cache isolation. New tests map to LIFE requirements or the explicit edge cases above. Repository AGENTS.md invariants and the skill's coding-principles checklist were applied.

Human/device UAT was not performed; no paid turn, live compaction, deployment, publication or merge was attempted. Optional events remain conditional on the pinned backend transport, as documented. This verdict establishes the automated local acceptance boundary, not live-account emission frequency.

## Traceability recommendation

Mark LIFE-01 through LIFE-35 Verified, T6 complete and T8 verified when the orchestrator records this report and final gate evidence. Keep the capability limits explicit in the roadmap/API documentation. The source and test implementation is ready for that acceptance-document step.

Deterministic completion gate: `python3 /home/mateuscmarim/.config/marim/skills/tlc-spec-driven/scripts/validate_state.py cli-lifecycle-parity` reported 0 errors.
